# Run this command

Follow the project rules in `.cursor/rules/`. Make changes that are small, clear, and consistent with the existing codebase. Prefer editing existing files over adding new ones unless the task requires it.
